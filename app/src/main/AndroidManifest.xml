<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity2"
            android:exported="false" />
        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Amazon Sign In Activity -->
        <activity
            android:name=".AmazonSignInActivity"
            android:exported="false" /> <!-- Flipkart Sign In Activity -->
        <activity
            android:name=".FlipkartSignInActivity"
            android:exported="false" /> <!-- Myntra Sign In Activity -->
        <activity
            android:name=".MyntraSignInActivity"
            android:exported="false" />
    </application>

</manifest>